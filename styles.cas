/* =========================================
   POCONO AI, LLC - MASTER STYLESHEET
   ========================================= */

@import url('https://fonts.googleapis.com/css2?family=Syne:wght@400;700;800&family=DM+Mono:ital,wght@0,300;0,400;0,500;1,300&family=Fraunces:ital,wght@0,300;0,600;1,300;1,600&display=swap');

:root { 
    --bg-black: #000000; 
    --bg-dark: #051622; 
    --card-dark: #0A2635; 
    --ai-blue: #00A3FF; 
    --teal: #188B96; 
    --white: #FFFFFF; 
    --light-text: #D0DDE6; 
    --muted: #5a7a8a;
}

/* Base Rules */
body { 
    font-family: 'DM Mono', monospace;
    margin: 0; 
    padding: 0; 
    background-color: var(--bg-dark); 
    color: var(--white); 
    line-height: 1.6; 
    overflow-x: hidden;
}
h1, h2, h3, h4, h5, h6, .cta-button { font-family: 'Syne', sans-serif; }
*:focus-visible { outline: none !important; box-shadow: 0 0 0 2px var(--ai-blue) !important; }
*:focus:not(:focus-visible) { outline: none !important; box-shadow: none !important; }

/* Header */
header {
    background-color: var(--bg-black);
    display: flex;
    justify-content: space-between;
    align-items: center;
    padding: 10px 2.5%;
    position: sticky;
    top: 0;
    z-index: 1000;
    height: 70px;
    border-bottom: 1px solid rgba(255,255,255,0.1);
    gap: 16px;
}
.logo {
    text-decoration: none;
    font-size: 1.5rem;
    font-weight: 800;
    color: var(--white);
    display: flex;
    align-items: center;
    font-family: -apple-system, BlinkMacSystemFont, 'Segoe UI', Roboto, Helvetica, Arial, sans-serif;
    gap: 8px;
    flex-shrink: 0;
    white-space: nowrap;
    letter-spacing: 0;
}
.logo img { height: 34px; width: auto; display: block; flex-shrink: 0; }
.logo span { color: var(--ai-blue); margin-left: 3px; }

/* Hamburger */
.menu-toggle {
    display: flex;
    flex-direction: column;
    gap: 5px;
    cursor: pointer;
    z-index: 1001;
    background: transparent;
    border: none;
    padding: 0;
}
.menu-toggle span { width: 25px; height: 3px; background-color: var(--white); transition: all 0.3s ease; }

/* Mobile nav panel */
#nav-menu {
    display: none;
    position: absolute;
    top: 70px;
    right: 2.5%;
    background: var(--bg-black);
    width: 240px;
    box-shadow: 0 10px 30px rgba(0,0,0,0.8);
    border-radius: 0 0 6px 6px;
    padding: 8px 0;
    border: 1px solid rgba(255,255,255,0.1);
}
#nav-menu.active { display: block; }
.nav-links { list-style: none; padding: 0; margin: 0; }

/* Mobile top-level links */
.nav-links > li > a {
    text-decoration: none;
    color: var(--white);
    font-weight: 500;
    padding: 11px 16px;
    display: block;
    border-bottom: 1px solid rgba(255,255,255,0.05);
    text-transform: uppercase;
    font-size: 0.85em;
    letter-spacing: 1px;
    font-family: 'Syne', sans-serif;
}

/* Dropdown */
.nav-dropdown { position: relative; }
.nav-dropdown-toggle {
    cursor: pointer;
    background: none;
    border: none;
    border-bottom: 1px solid rgba(255,255,255,0.05);
    color: var(--white);
    font-family: 'Syne', sans-serif;
    font-size: 0.85em;
    font-weight: 500;
    text-transform: uppercase;
    letter-spacing: 1px;
    padding: 11px 16px;
    display: flex;
    align-items: center;
    gap: 6px;
    width: 100%;
    text-align: left;
}
.nav-dropdown-toggle:hover { color: var(--ai-blue); }
.nav-dropdown-toggle .arrow {
    font-size: 0.7em;
    transition: transform 0.2s ease;
    display: inline-block;
    margin-left: auto;
}
.nav-dropdown-toggle.open .arrow { transform: rotate(180deg); }

/* Mobile dropdown submenu — inline expand */
.nav-dropdown-menu {
    display: none;
    background: rgba(0,0,0,0.4);
    border-top: 1px solid rgba(255,255,255,0.05);
}
.nav-dropdown-menu.open { display: block; }
.nav-dropdown-menu a {
    display: block;
    padding: 10px 28px;
    color: var(--light-text);
    text-decoration: none;
    font-family: 'Syne', sans-serif;
    font-size: 0.8em;
    text-transform: uppercase;
    letter-spacing: 1px;
    border-bottom: 1px solid rgba(255,255,255,0.04);
    transition: color 0.2s;
}
.nav-dropdown-menu a:last-child { border-bottom: none; }
.nav-dropdown-menu a:hover { color: var(--ai-blue); }

/* Desktop nav (900px+) */
@media (min-width: 900px) {
    .menu-toggle { display: none; }
    #nav-menu {
        display: flex;
        position: static;
        width: auto;
        background: transparent;
        box-shadow: none;
        border: none;
        padding: 0;
        align-items: center;
    }
    .nav-links { display: flex; gap: 8px; align-items: center; flex-wrap: nowrap; }
    .nav-links > li > a { border: none; padding: 0 3px; font-size: 0.76em; white-space: nowrap; transition: color 0.3s ease; }
    .nav-links > li > a:hover { color: var(--ai-blue); }
    .nav-dropdown-toggle { border: none; padding: 0 3px; font-size: 0.76em; white-space: nowrap; width: auto; }

    /* Desktop flyout — click-triggered via JS, not CSS hover */
    .nav-dropdown { position: relative; }
    .nav-dropdown-menu {
        position: absolute;
        top: 100%;          /* flush against button — no gap */
        right: 0;
        background: var(--bg-black);
        border: 1px solid rgba(255,255,255,0.15);
        border-radius: 6px;
        box-shadow: 0 16px 40px rgba(0,0,0,0.9);
        min-width: 185px;
        z-index: 2000;      /* well above header z-index:1000 */
        /* Invisible top padding bridges any sub-pixel gap between
           button and menu so the mouse never falls into dead space */
        padding-top: 6px;
        margin-top: -2px;
    }
    /* Remove CSS-hover trigger — JS handles open/close exclusively */
    /* This prevents the flicker where menu vanishes as mouse travels */
    .nav-dropdown-menu a {
        padding: 11px 18px;
        font-size: 0.82em;
        color: var(--white);
        border-bottom: 1px solid rgba(255,255,255,0.06);
    }
    .nav-dropdown-menu a:last-child { border-bottom: none; }
    .nav-dropdown-menu a:hover { color: var(--ai-blue); background: rgba(255,255,255,0.04); }
    .nav-dropdown-toggle.open { color: var(--ai-blue); }
}
@media (min-width: 1100px) {
    .nav-links { gap: 16px; }
    .nav-links > li > a { font-size: 0.83em; }
    .nav-dropdown-toggle { font-size: 0.83em; }
}
@media (min-width: 1300px) {
    .nav-links { gap: 22px; }
    .nav-links > li > a { font-size: 0.88em; }
    .nav-dropdown-toggle { font-size: 0.88em; }
}

/* Global Elements */
main { max-width: 1000px; margin: 0 auto; padding: 40px 10px; }
.cta-button { display: inline-block; background-color: var(--teal); color: var(--white); padding: 16px 32px; text-decoration: none; font-weight: bold; border-radius: 4px; margin: 10px; transition: 0.3s; cursor: pointer; border: none; text-transform: uppercase; letter-spacing: 1px; }
.cta-button:hover { filter: brightness(1.1); transform: translateY(-2px); }
.btn-submit { display: inline-block; background-color: var(--teal); color: var(--white) !important; padding: 16px 32px; text-decoration: none; font-weight: bold; border-radius: 4px; margin: 10px; transition: 0.3s; cursor: pointer; border: none; text-transform: uppercase; letter-spacing: 1px; font-family: 'Syne', sans-serif; }
.btn-submit:hover { filter: brightness(1.1); }
.cta-button-light { background-color: transparent !important; color: var(--white) !important; border: 2px solid var(--white) !important; }
.cta-button-light:hover { background-color: rgba(255,255,255,0.1) !important; filter: none; }


/* dl-btn — download button variant used on specs page */
.dl-btn { display: inline-block; background-color: var(--ai-blue); color: var(--white) !important; padding: 14px 28px; text-decoration: none; font-weight: bold; border-radius: 4px; margin: 8px; transition: 0.3s; cursor: pointer; text-transform: uppercase; letter-spacing: 1px; font-family: 'Syne', sans-serif; }
.dl-btn:hover { filter: brightness(1.1); }

/* linkedin-btn — LinkedIn CTA variant */
.linkedin-btn { background-color: #0077b5 !important; }
.linkedin-btn:hover { background-color: #005e8e !important; filter: none; }

/* Footer */
footer { text-align: center; padding: 40px 20px; font-size: 0.85em; color: var(--light-text); border-top: 1px solid var(--card-dark); margin-top: 50px; }
footer a { color: var(--teal); text-decoration: none; text-transform: uppercase; letter-spacing: 1px; font-family: 'DM Mono', monospace; transition: 0.3s; }
footer a:hover { color: var(--ai-blue); }
